# Day-one smoke train/eval needs torch (CPU is fine) + pyyaml + pytest + numpy.
torch>=2.2
pyyaml>=6.0
pytest>=7.0
numpy>=1.24

# For real runs (uncomment as needed)
# open_clip_torch>=2.24     # frozen CLIP ViT-B/16 backbone + activations
# transformers>=4.44        # alt backbone / processors
# datasets>=2.19            # ImageNet + ImageNet-R/Sketch/C/ObjectNet shifts
# scikit-learn>=1.3         # linear probes / TCAV-style concept directions
# einops>=0.7
