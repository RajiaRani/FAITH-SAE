# =============================================================================
# requirements.txt  --  Milestone 2 (FAITH-SAE data pipeline)
# Author: Rajia Rani
#
# This file is ADDITIVE: it lists what THIS milestone needs on top of the
# project's root requirements.txt. The DEFAULT offline path (step1 + step2)
# uses ONLY the first block, all of which ships with the system /usr/bin/python3
# on this machine -- so you can run today with NO installs.
# =============================================================================

# ---- Offline default path (already available; listed for completeness) ------
numpy>=1.24          # the activation arrays + all math
matplotlib>=3.5      # the EDA + OOD figures
scikit-learn>=1.3    # PCA for the 2-D scatter
pandas>=1.5          # the per-dimension EDA table -> CSV
pyyaml>=6.0          # reads config.yaml
torch>=2.2           # used by src/utils.set_seed (CPU is fine)

# ---- REAL RUN (M2) ONLY -- do NOT install for the offline milestone ---------
# Uncomment these only when you switch step1.build_real_clip_bank() to the real
# CLIP ViT-B/16 + ImageNet pipeline (see README §4). They pull large downloads.
# open_clip_torch>=2.24   # frozen CLIP ViT-B/16 backbone + image preprocessing
# datasets>=2.19          # ImageNet + ImageNet-R/Sketch/C/ObjectNet loaders
# pillow>=9.0             # image decoding for the real forward pass
