# Milestone 3 (Baseline) — additive requirements.
# These are ON TOP OF the repo-root requirements.txt. The offline default path
# (synthetic activation bank, CPU) needs only these four, all already present in
# the project's base environment:
torch>=2.2          # the SAE, tensors, autograd, SVD
pyyaml>=6.0         # read config.yaml
numpy>=1.24         # array math for selection/plotting
matplotlib>=3.5     # the reconstruction-loss curve PNG (step 1)

# ---------------------------------------------------------------------------
# REAL RUN (M3) — only if you train the SAE on REAL CLIP ViT-B/16 activations
# (see the "REAL RUN (M3)" note in the README). Do NOT install for the offline
# default; nothing here is downloaded by the default path.
# open_clip_torch>=2.24   # frozen CLIP ViT-B/16 backbone to dump real activations
# datasets>=2.19          # ImageNet (and the OOD shift sets) to feed the backbone
# scikit-learn>=1.3       # learned linear probes for the concept readouts
